VITE_API_URL=http://localhost:4000/api

# Google Sign-In client id (same value as the backend's GOOGLE_CLIENT_ID).
# Leave blank to hide the "Google bilan kirish" button entirely.
VITE_GOOGLE_CLIENT_ID=
